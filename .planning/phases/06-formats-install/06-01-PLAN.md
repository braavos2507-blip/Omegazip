---
phase: 06-formats-install
plan: 01
type: plan
wave: 1
depends_on:
  - 05-01
files_modified:
  - docs/FORMATS.md
  - docs/INSTALL.md
  - README.md
  - ui/index.html
  - ui-android/index.html
autonomous: true
requirements:
  - FMT-01
  - FMT-02
  - FMT-03
  - INST-01
  - INST-02
user_setup:
  - Редактор markdown; при правке UI — локальный `npm run tauri dev` для проверки баннера 7-Zip.

must_haves:
  truths:
    - В репозитории есть матрица форматов (FORMATS.md) с колонками нативно / через 7-Zip / не поддерживается.
    - Стратегия поставки 7-Zip описана явно (внешний бинарник, авто-поиск — без обязательного bundling в v1.1).
    - Один понятный путь установки для пользователя (INSTALL.md) и краткая связка в README.
  artifacts:
    - docs/FORMATS.md
    - docs/INSTALL.md
    - Обновление README.md (ссылки, при необходимости сжатие дублирования с README → FORMATS/INSTALL)
  key_links:
    - src/compat.rs — источник истины по натив vs 7z
    - seven_zip_status / seven_zip_install_howto — INST-02, FMT-03
---

<objective>
Закрыть **FMT-01 — FMT-03** и **INST-01 — INST-02** для майлстоуна v1.1: **матрица форматов** с ориентиром «как у топовых» (через сравнение возможностей с нативом + 7-Zip), **план/ограничения** по явным пробелам, **документ установки** и стратегия **7-Zip**; **первый запуск** GUI — компактный статус + ссылка на документацию без перегруза.

Purpose: документация и лёгкий UX; без обязательного изменения ядра сжатия, если матрица подтверждает текущее поведение.

Output: `docs/FORMATS.md`, `docs/INSTALL.md`, правки README и опционально UI-баннер.
</objective>

<execution_context>
@/Users/renat/Documents/Project/OmegaZip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renat/Documents/Project/OmegaZip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-formats-install/06-01-RESEARCH.md
@src/compat.rs
@README.md
@ui/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/FORMATS.md (FMT-01, часть FMT-02)</name>
  <files>docs/FORMATS.md</files>
  <read_first>06-01-RESEARCH.md, src/compat.rs (заголовок, ошибки ZIP, RAR note), README раздел про форматы</read_first>
  <action>Создать **матрицу** в табличном виде (Markdown): формат / расширение → **чтение** (нативно / только 7-Zip / нет) → **запись** (аналогично). Включить группы: `.oz`, ZIP/tar/gz/xz/bz2/zst, CAB, **7z**, **RAR**, ISO/WIM/MSI/DMG (как типично через 7-Zip). Добавить подраздел **«Ориентир "топ‑5"»**: 1–2 абзаца, что паритет с WinRAR/WinZip и т.д. для «экзотики» достигается через **совместимость с 7-Zip**, а не дублированием кодеков; OmegaZip не создаёт RAR. Отдельный подраздел **«Известные ограничения»** (FMT-02): зашифрованный ZIP, Android без 7-Zip, ссылки на `omegazip deps`.</action>
  <verify>Файл существует; есть таблица и подраздел ограничений.</verify>
  <acceptance_criteria>
    - FMT-01: есть матрица нативно vs 7-Zip vs N/A
    - FMT-02: явные пробелы перечислены или помечены «не планируется в v1.1»
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: docs/INSTALL.md (FMT-03, INST-01)</name>
  <files>docs/INSTALL.md</files>
  <read_first>README, CONTEXT-MENU.md (кратко), tauri/README если есть</read_first>
  <action>Создать **INSTALL.md**: (1) **OmegaZip** — типовые пути: разработчик (`cargo build`, `npm run tauri …` по факту репозитория); конечный пользователь — установка из артефакта сборки (msi/dmg/AppImage/deb — формулировка «см. релиз», без выдуманных URL). Размер/зависимости — кратко (Rust runtime внутри bundle, и т.д. по факту). (2) **7-Zip / p7zip** — FMT-03: стратегия **не bundle** в репозитории; автообнаружение в Program Files / PATH; команды установки по ОС (синхронно с `seven_zip_install_howto`). (3) Ссылка на **FORMATS.md** и **FILE-ASSOCIATIONS** / **CONTEXT-MENU** по смыслу.</action>
  <verify>Файл не противоречит README; `grep -q INSTALL README.md` после Task 4.</verify>
  <acceptance_criteria>
    - INST-01: один читаемый сценарий «как поставить» + раздел про 7-Zip
    - FMT-03: стратегия поставки 7-Zip зафиксирована
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: INST-02 — первый запуск, компактность</name>
  <files>ui/index.html, ui-android/index.html (при идентичном баннере)</files>
  <read_first>логика `seven_zip_status` / разметка баннера</read_first>
  <action>Если баннер при отсутствии 7-Zip показывает **слишком длинный** блок текста в одном `<pre>`: сократить до 2–4 строк + ссылка «Подробнее: **docs/INSTALL.md**» (или якорь README). Сохранить `install_howto` в CLI (`deps`) без удаления. Не ломать классы `deps-banner`, `deps-warn`.</action>
  <verify>Ручной dev: открыть UI без 7-Zip в PATH — баннер читаемый; с 7-Zip — зелёный.</verify>
  <acceptance_criteria>
    - Первый запуск не перегружен; пользователь видит путь к документации
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: README — навигация</name>
  <files>README.md</files>
  <read_first>README.md текущий</read_first>
  <action>Добавить строки со ссылками на **docs/FORMATS.md** и **docs/INSTALL.md** (рядом с FILE-ASSOCIATIONS / CONTEXT-MENU). При дублировании большого куска про 7-Zip — заменить на 1 абзац + «подробнее в FORMATS/INSTALL», не удаляя полезность для быстрого чтения.</action>
  <verify>grep -E 'FORMATS|INSTALL' README.md</verify>
  <acceptance_criteria>
    - README содержит ссылки на оба новых документа
  </acceptance_criteria>
</task>

</tasks>

<verification_notes>
После выполнения: `06-01-SUMMARY.md`, обновить **VALIDATION.md**, **STATE.md** (Phase 6 Ready to execute → Completed после execute).
</verification_notes>
