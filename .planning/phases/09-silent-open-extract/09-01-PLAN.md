---
phase: 09-silent-open-extract
plan: 01
type: plan
wave: 1
depends_on:
  - 04-01
  - 05-01
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/tauri.conf.json
  - ui/index.html
  - docs/FILE-ASSOCIATIONS.md
  - docs/SILENT-EXTRACT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - GAP-01
user_setup:
  - Локально: `cargo test` (корень), `cd src-tauri && cargo check`; ручная проверка двойного щелчка по `.zip`/`.oz` на целевой ОС.

must_haves:
  truths:
    - Один поддерживаемый архив из ОС → распаковка в каталог архива без обязательного видимого главного окна при успехе.
    - Пароль / фатальная ошибка → главное окно доступно с уже подставленным архивом (и понятным сообщением).
    - Список «это архив для silent» согласован между логикой silent и GUI (один источник или явная синхронизация).
  artifacts:
    - docs/SILENT-EXTRACT.md (поведение, настройка, исключения)
    - Обновление docs/FILE-ASSOCIATIONS.md (1 раздел)
  key_links:
    - RunEvent::Opened / Ready, WebviewWindow::hide/show
    - omegazip::decompress_any_to_path_with_options
---

<objective>
Закрыть **GAP-01**: при открытии приложения с **одним** путём к поддерживаемому архиву выполнить **автоматическую** распаковку рядом с архивом **без** показа главного окна при успешном завершении.

Purpose: соответствие ТЗ «двойной щелчок без обращения к интерфейсу архиватора» в типичном сценарии.

Output: логика в Rust (setup + Opened/Ready), опционально переключатель в UI, документация, обновление REQ/STATE по готовности.
</objective>

<execution_context>
@/Users/renat/Documents/Project/OmegaZip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renat/Documents/Project/OmegaZip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-silent-open-extract/09-01-RESEARCH.md
@src-tauri/src/lib.rs
@ui/index.html
@docs/FILE-ASSOCIATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Общая функция «архив ли путь» в Rust</name>
  <files>src-tauri/src/lib.rs или новый модуль src-tauri/src/archive_kind.rs; при возможности src/lib.rs</files>
  <read_first>ui/index.html — isExtractArchivePath; omegazip — какие форматы поддерживает decompress_any</read_first>
  <action>Реализовать функцию вида `fn is_supported_archive_path(p: &Path) -> bool` по расширению (и при необходимости по существованию файла), покрывающую тот же набор, что ожидает распаковка через `decompress_any_to_path`. Избежать расхождения с GUI: либо дублировать список с комментарием «синхронизировать с UI», либо экспортировать константу/модуль из ядра. Задокументировать в RESEARCH или в коде списком суффиксов.</action>
  <verify>cargo check -p omegazip-app</verify>
  <acceptance_criteria>
    - Один источник правды или явная пометка синхронизации с `isExtractArchivePath`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Ветка silent-extract в lifecycle Tauri</name>
  <files>src-tauri/src/lib.rs, при необходимости tauri.conf.json (window visible по умолчанию не ломать обычный запуск)</files>
  <read_first>09-01-RESEARCH.md, текущий run() с RunEvent</read_first>
  <action>(1) Ввести чтение настройки «silent extract on open» (по умолчанию **включено**): через `tauri-plugin-store`, либо простой JSON в `app.path().app_config_dir()`, либо env `OMEGAZIP_NO_SILENT_EXTRACT=1` для отладки — зафиксировать в доке. (2) В `setup` после получения handle окна: если ровно один путь, файл существует, `is_supported_archive_path`, настройка включена — **скрыть** главное окно (`get_webview_window("main")` → `hide()`). (3) Вызвать распаковку: `dest = parent_dir(archive)`; использовать существующую логику ядра **без** пароля в первом проходе; при ошибке, указывающей на пароль (если ядро различает — иначе любая ошибка) → `show()` окна и `emit("open-files", vec![path])` + при необходимости второе событие для ошибки. (4) При успехе — `app.exit(0)`. (5) **macOS:** продублировать вызов той же логики внутри `RunEvent::Opened` (пути из urls), чтобы silent срабатывал и когда событие приходит после старта. (6) **Windows/Linux:** если `setup` раньше не видит args, оставить обработку в `Ready` как сейчас, но с приоритетом silent до показа UI. Убедиться, что обычный запуск без аргументов окно **видимо**.</action>
  <verify>cargo check -p omegazip-app; ручной тест на одной ОС</verify>
  <acceptance_criteria>
    - Двойной щелчок по тестовому `.zip` распаковывает без видимого главного окна (если нет пароля)
    - Запуск без аргументов — окно как раньше
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: UI — переключатель и событие ошибки</name>
  <files>ui/index.html</files>
  <read_first>секция настроек / статусы</read_first>
  <action>Добавить в настройки (или компактный блок) чекбокс «Распаковывать сразу при открытии архива» (или эквивалент), сохраняющий флаг в тот же механизм, что читает Rust. Если событие `silent-extract-failed` (или общее) — показать текст в статусе распаковки. Не дублировать распаковку в JS при успешном silent (только при fallback на окно).</action>
  <verify>Ручная проверка переключателя</verify>
  <acceptance_criteria>
    - Пользователь может отключить silent без правки файлов
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Документация</name>
  <files>docs/SILENT-EXTRACT.md (новый), docs/FILE-ASSOCIATIONS.md, README.md (одна строка)</files>
  <read_first>FILE-ASSOCIATIONS.md</read_first>
  <action>Описать: когда срабатывает silent, пароль, несколько файлов, отключение, ограничения (нет прогресс-бара в silent v1). Ссылка из FILE-ASSOCIATIONS и README.</action>
  <verify>grep -q SILENT README.md docs/FILE-ASSOCIATIONS.md</verify>
  <acceptance_criteria>
    - Пользователь и тестировщик могут воспроизвести сценарий по доке
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 5: Закрытие GAP-01 в REQUIREMENTS + STATE</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>После реализации: отметить **GAP-01** выполненным; обновить traceability; STATE — фаза 9 completed / следующая фаза 10.</action>
  <verify>Визуально чеклист</verify>
</task>

</tasks>
