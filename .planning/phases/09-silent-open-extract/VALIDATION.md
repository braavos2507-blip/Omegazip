# Validation — Phase 9 (GAP-01)

**Requirements:** GAP-01

## Чеклист

- [ ] Один `.zip`/`.oz` без пароля по ассоциации → файлы появляются рядом с архивом, главное окно **не** остаётся на экране при успехе
- [ ] Запуск OmegaZip без аргументов → главное окно **видимо**
- [ ] Архив с паролем → окно открывается, архив в поле распаковки, сообщение о пароле (или эквивалент)
- [ ] Отключение в настройках → поведение как до фазы (только `open-files` + ручная кнопка)
- [ ] `cargo test` (корень) и `cargo check` в `src-tauri` проходят
- [ ] `docs/SILENT-EXTRACT.md` и ссылки из FILE-ASSOCIATIONS / README

## Команды

```bash
cargo test
cd src-tauri && cargo check
```

## Ручной тест

Двойной щелчок по тестовому архиву в проводнике/Finder на доступной ОС.
