---
phase: 05-context-menu-background
plan: 01
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - docs/CONTEXT-MENU.md
  - docs/FILE-ASSOCIATIONS.md
  - README.md
  - scripts/install-context-menu.sh
  - scripts/context-menu-windows.reg.example
  - src-tauri/src/lib.rs
  - ui/index.html
  - ui-android/index.html
autonomous: true
requirements:
  - SHELL-02
  - SHELL-03
  - PATH-02
  - PATH-03
user_setup:
  - macOS: права на запись в `~/Library/Services`, перезапуск Finder.
  - Windows/Linux: установленный `omegazip` в PATH или полный путь в reg/desktop-файле.

must_haves:
  truths:
    - Пользователь macOS может установить через скрипт минимум два сервиса сжатия — в .oz и в .zip — без открытия окна OmegaZip.
    - Скрипты кладут результат в каталог исходного файла с именем по правилу stem + целевое расширение (PATH-02, PATH-03).
    - В репозитории есть инструкции для Windows и Linux по добавлению действий ПКМ (хотя бы шаблоны).
  artifacts:
    - Обновлённый scripts/install-context-menu.sh (или рядом install-context-menu-zip.workflow)
    - docs/CONTEXT-MENU.md (единый документ)
    - Опционально scripts/context-menu-windows.reg.example
  key_links:
    - omegazip CLI compress → .zip и .oz
---

<objective>
Расширить сценарий «ПКМ → сжать без главного окна»: **.oz** и **.zip**, с корректным именем и папкой; задокументировать Windows/Linux; зафиксировать SHELL-03 как «CLI-first без окна», трей — follow-up.

Purpose: закрыть SHELL-02, PATH-02, PATH-03; SHELL-03 — в минимальном объёме (без обязательного трея в этом плане).
Output: обновлённые скрипты macOS, docs/CONTEXT-MENU.md, примеры для Win/Linux.
</objective>

<execution_context>
@/Users/renat/Documents/Project/OmegaZip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renat/Documents/Project/OmegaZip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-context-menu-background/05-01-RESEARCH.md
@scripts/install-context-menu.sh
@docs/FILE-ASSOCIATIONS.md
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Единый документ docs/CONTEXT-MENU.md</name>
  <files>docs/CONTEXT-MENU.md</files>
  <read_first>docs/FILE-ASSOCIATIONS.md, 05-01-RESEARCH.md</read_first>
  <action>Создать `docs/CONTEXT-MENU.md`: цель (Сжать в .oz / .zip без окна), macOS (ссылка на `install-context-menu.sh`), Windows (рекомендация: reg или «Отправить» + пример вызова `omegazip compress`), Linux (фрагмент `.desktop` `Actions=`). Указать требование PATH-02/03 (папка и имя stem+расширение).</action>
  <verify>Файл существует, три подзаголовка платформ.</verify>
  <acceptance_criteria>
    - docs/CONTEXT-MENU.md создан
    - Упомянуты оба формата .oz и .zip
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Расширить install-context-menu.sh — сжатие в .zip и имена stem</name>
  <files>scripts/install-context-menu.sh</files>
  <read_first>scripts/install-context-menu.sh полностью, src/main.rs (compress в .zip)</read_first>
  <action>(1) Вынести общую логику имени выхода: для файла `dir/base.ext` → `dir/base.oz` и `dir/base.zip` (stem = имя без последнего расширения; если нет точки — `base.zip`/`base.oz`). Для **директории** `dir/MyFolder` → `dir/MyFolder.oz` / `dir/MyFolder.zip`. (2) Добавить третий workflow «Сжать в ZIP (OmegaZip)» с тем же механизмом передачи файлов в shell, вызов `"$OZ" compress "$f" "$out"` где `out` — `.zip`. (3) Обновить существующий compress workflow на stem-имена для .oz. (4) По возможности унифицировать распаковку для не только .oz (опционально в этом плане — только если малый diff; иначе отдельная задача в SUMMARY как gap).</action>
  <verify>shellcheck scripts/install-context-menu.sh 2>/dev/null || bash -n scripts/install-context-menu.sh</verify>
  <acceptance_criteria>
    - После установки скрипта в Services есть отдельные пункты для .oz и .zip ИЛИ два workflow с понятными именами
    - Имена выходных файлов соответствуют PATH-03 (stem)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Пример Windows reg / cmd</name>
  <files>scripts/context-menu-windows.reg.example</files>
  <read_first>docs/CONTEXT-MENU.md (черновик из Task 1)</read_first>
  <action>Создать **пример** `scripts/context-menu-windows.reg.example` с комментариями в начале файла (.reg не поддерживает комментарии — использовать блок README в CONTEXT-MENU и минимальный reg без комментариев ИЛИ отдельный `.md` с блоком кода). Предпочтительно: `scripts/context-menu-windows.reg.example` как текстовый шаблон с плейсхолдером `C:\\Path\\To\\omegazip.exe` и двумя ключами для «CompressOZ» / «CompressZip». Документировать риски (UAC, пути с пробелами).</action>
  <verify>Файл не ломает сборку; это пример, не автозапуск.</verify>
  <acceptance_criteria>
    - В репозитории есть воспроизводимый шаблон для Windows
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: GUI — начальный каталог диалога «Куда сохранить» (PATH-02)</name>
  <files>src-tauri/src/lib.rs, ui/index.html, ui-android/index.html</files>
  <read_first>Tauri 2 dialog plugin: set_directory или аналог для save dialog</read_first>
  <action>Если API плагина позволяет: при вызове `pick_save_file` передавать **опционально** каталог по умолчанию (родитель `compressSrc`). Потребуется новая сигнатура команды, например `pick_save_file(default_name, default_dir)` или структура. Фронт: при нажатии «Куда сохранить» передавать `parentDirForPath(compressSrc)` если compressSrc задан. Если API недоступен в текущей версии плагина — задокументировать ограничение в CONTEXT-MENU.md и завести gap в SUMMARY (не блокировать план).</action>
  <verify>cargo check</verify>
  <acceptance_criteria>
    - Либо диалог открывается в папке источника, либо в docs явно записано «ожидает обновление tauri-plugin-dialog»
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: README и ссылки</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>Добавить строку со ссылкой на `docs/CONTEXT-MENU.md` рядом с FILE-ASSOCIATIONS.</action>
  <verify>grep CONTEXT-MENU README.md</verify>
  <acceptance_criteria>
    - README содержит ссылку на CONTEXT-MENU.md
  </acceptance_criteria>
</task>

</tasks>

<verification_notes>
После выполнения: `05-01-SUMMARY.md`, обновить `VALIDATION.md`, при необходимости `STATE.md`.
SHELL-03 (трей): если не реализован — явно «deferred» в SUMMARY.
</verification_notes>
