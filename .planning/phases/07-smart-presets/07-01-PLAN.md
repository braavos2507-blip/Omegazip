---
phase: 07-smart-presets
plan: 01
type: plan
wave: 1
depends_on:
  - 06-01
files_modified:
  - src/smart_preset.rs
  - src/lib.rs
  - src/main.rs
  - src-tauri/src/lib.rs
  - ui/index.html
  - ui-android/index.html
  - docs/SMART-PRESETS.md
  - docs/FORMATS.md
  - README.md
autonomous: true
requirements:
  - SMART-01
  - SMART-02
user_setup:
  - Локально: `cargo test` после изменений Rust; при правке UI — `npm run tauri dev`.

must_haves:
  truths:
    - Таблица эвристик «расширение / категория → Preset» задокументирована и реализована в коде (или вынесена в один модуль с константами).
    - Lossless-типы не сопоставляются с режимами, нарушающими без потерь (ядро .oz остаётся lossless).
    - Есть тесты минимум на 2–3 класса входов (например только `.txt`, только `.mp4`, смешанная папка).
  artifacts:
    - docs/SMART-PRESETS.md (или эквивалент)
    - src/*.rs — логика подсказки пресета
    - ui/index.html, ui-android/index.html — опция «Авто» или эквивалент
  key_links:
    - pipeline::Preset, apply_preset
    - analyzer / codec — уже классифицируют Media; план не дублирует Store на уровне блока, а дополняет **уровнем пресета**
---

<objective>
Закрыть **SMART-01** и **SMART-02**: автоматический выбор **профиля .oz** (`Preset`) по расширениям и простым правилам для папок, с **документацией**, **тестами** и **UI/CLI** без изменения формата архива.

Purpose: меньше ручных ошибок (ultra на каталоге с JPEG); честное поведение для кода и медиа.

Output: модуль эвристик, документ, интеграция в селектор пресетов, опционально `compress --preset auto`.
</objective>

<execution_context>
@/Users/renat/Documents/Project/OmegaZip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renat/Documents/Project/OmegaZip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-smart-presets/07-01-RESEARCH.md
@src/pipeline.rs
@src/analyzer.rs
@src/codec.rs
@ui/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Модуль эвристик + тесты</name>
  <files>src/lib.rs, новый файл src/smart_preset.rs (или аналог)</files>
  <read_first>07-01-RESEARCH.md, Preset::from_str, apply_preset</read_first>
  <action>Реализовать **публичную** функцию вида `suggested_preset_for_path(input: &Path) -> Preset` и при необходимости `suggested_preset_for_paths(files: &[PathBuf]) -> Preset` для обхода папки (ограничить глубину/количество файлов для производительности, задокументировать). Таблица SMART-01: например медиа/уже сжатые расширения (`.mp4`, `.jpg`, `.zip`, `.gz`, …) → **Fast**; исходники и текст (`.rs`, `.c`, `.md`, `.txt`, `.json`, …) → **Balanced**; диски/большие бинарные дампы (`.iso`, `.img`, `.vmdk` — по согласованию) → **Fast** или **Balanced**; смешанное множество → **Balanced**. Уточнить список в коде через `match`/`HashSet` расширений. **SMART-02:** не вводить lossy; для «медиа» выбирать режим с минимальным solid/chunk-оверходом (Fast), опираясь на существующую семантику `Preset`. Юнит-тесты: минимум 3 сценария.</action>
  <verify>cargo test</verify>
  <acceptance_criteria>
    - `cargo test` проходит
    - Таблица расширений не пустая и покрывает примеры из ТЗ (код/текст vs медиа)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Экспорт для Tauri и опционально CLI</name>
  <files>src/lib.rs, src/main.rs (опционально), src-tauri/src/lib.rs</files>
  <read_first>существующие `#[tauri::command]`</read_first>
  <action>Добавить команду **`suggest_compress_preset(path: String) -> String`** (возвращает имя пресета `fast|balanced|max|ultra` для UI). Либо возвращать структуру с полем `preset` и `reason` (кратко, для подсказки). Опционально: в `main.rs` поддержать `--preset auto` для `compress`, вызывая ту же логику.</action>
  <verify>cargo check, при наличии — ручной вызов команды</verify>
  <acceptance_criteria>
    - GUI может запросить подсказку без дублирования таблицы в JS
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: UI — «Авто» и синхронизация с источником</name>
  <files>ui/index.html, ui-android/index.html</files>
  <read_first>btn-compress-src, compressSrc, compress-preset, invoke compress_advanced</read_first>
  <action>Добавить в `<select id="compress-preset">` значение **`auto`** (подпись вроде «Авто (по типу файлов)»). При выборе источника (`compressSrc` обновлён) и активной опции **auto**: вызвать `suggest_compress_preset`, выставить **фактический** пресет для `compress_advanced` (передавать в Rust разрешённое имя пресета, не `auto`). Если пользователь выбрал не-auto — не перезаписывать до следующего явного выбора auto. Альтернатива (если проще): только кнопка «Подставить рекомендуемый пресет» — согласовать в SUMMARY при отклонении от авто-режима.</action>
  <verify>Ручная проверка: выбрать папку с `.rs` vs с `.mp4`, убедиться в разном пресете при режиме auto</verify>
  <acceptance_criteria>
    - Пользователь видит режим Авто и понимает, что он влияет только на .oz
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: docs/SMART-PRESETS.md</name>
  <files>docs/SMART-PRESETS.md, README.md (одна строка ссылки)</files>
  <read_first>07-01-RESEARCH.md</read_first>
  <action>Создать документ с **таблицей эвристик** (как в коде), пояснением SMART-02 (lossless, медиа), ссылкой на `analyzer`/`codec` для блоков. Добавить в README короткую строку со ссылкой.</action>
  <verify>grep SMART-PRESETS README.md</verify>
  <acceptance_criteria>
    - Документ существует; SMART-01/02 прослеживаются текстом
  </acceptance_criteria>
</task>

</tasks>

<verification_notes>
После выполнения: `07-01-SUMMARY.md`, **VALIDATION.md**, **STATE.md**.
</verification_notes>
