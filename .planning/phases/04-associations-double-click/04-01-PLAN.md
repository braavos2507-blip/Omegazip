---
phase: 04-associations-double-click
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - SHELL-01
  - PATH-01
user_setup:
  - Установка OmegaZip из bundle (Windows/macOS/Linux) для проверки двойного щелчка на реальной машине.
  - Права администратора только если пользователь вручную применяет reg-скрипт Windows (опционально).

must_haves:
  truths:
    - При открытии одного архива через open-files в UI подставляется каталог распаковки по умолчанию = родительский каталог архива (если пользователь не выбирал другой).
    - В репозитории есть документ с шагами регистрации ассоциаций для Windows, macOS и Linux.
    - В tauri.conf.json расширен список fileAssociations минимум для zip и распространённых архивных суффиксов согласованно с UI isExtractArchivePath.
  artifacts:
    - docs/FILE-ASSOCIATIONS.md
    - .planning/phases/04-associations-double-click/04-01-RESEARCH.md (уже создан)
    - Изменения в ui/index.html и при необходимости ui-android/index.html (логика умолчания пути)
    - Изменения в src-tauri/tauri.conf.json (fileAssociations)
  key_links:
    - applyPaths / open-files в ui/index.html ↔ PATH-01
    - bundle.fileAssociations ↔ SHELL-01
---

<objective>
Сделать сценарий «открыл архив из проводника → OmegaZip предлагает распаковку в папку рядом с архивом» и зафиксировать, как пользователь включает ассоциации по двойному щелчку на трёх ОС.

Purpose: закрыть SHELL-01 (ассоциации + документация) и PATH-01 (умолчание каталога распаковки).
Output: документация, обновление списка ассоциаций в Tauri, правка фронта для default extract dest.
</objective>

<execution_context>
@/Users/renat/Documents/Project/OmegaZip/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renat/Documents/Project/OmegaZip/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-associations-double-click/04-01-RESEARCH.md

@src-tauri/src/lib.rs
@src-tauri/tauri.conf.json
@src-tauri/Info.plist
@ui/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Документация FILE-ASSOCIATIONS.md</name>
  <files>docs/FILE-ASSOCIATIONS.md</files>
  <read_first>.planning/phases/04-associations-double-click/04-01-RESEARCH.md, README.md</read_first>
  <action>Создать `docs/FILE-ASSOCIATIONS.md` на русском (кратко): (1) зачем нужны ассоциации; (2) Windows — что делает установщик Tauri / ручная смена «Открыть с помощью»; (3) macOS — назначение приложения по умолчанию для расширений; (4) Linux — xdg-mime / свойства файла; (5) ссылка на список расширений из кода/UI. Упомянуть ограничение: полная интеграция контекстного меню — фаза 5.</action>
  <verify>Файл существует, содержит подзаголовки Windows/macOS/Linux.</verify>
  <acceptance_criteria>
    - В репозитории есть `docs/FILE-ASSOCIATIONS.md`
    - Упомянут сценарий двойного щелчка и связь с `open-files`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Расширить bundle.fileAssociations в tauri.conf.json</name>
  <files>src-tauri/tauri.conf.json</files>
  <read_first>ui/index.html (функция isExtractArchivePath — список суффиксов для ориентира)</read_first>
  <action>Добавить в `bundle.fileAssociations` отдельные записи для минимум: `zip`, `7z`, `rar` (роль Viewer/Editor по смыслу «открыть в OmegaZip»). Сохранить существующую запись `oz`. Следовать схеме Tauri 2 (массив объектов с `ext`, `description`, `role`). Не добавлять десятки расширений в один объект без проверки сборки — при необходимости второй коммит после `cargo tauri build` smoke.</action>
  <verify>`cargo check` в `src-tauri` проходит.</verify>
  <acceptance_criteria>
    - В JSON присутствуют ассоциации для oz, zip, 7z, rar (как минимум)
    - Валидный JSON
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: PATH-01 — умолчание папки распаковки при open-files</name>
  <files>ui/index.html</files>
  <read_first>ui/index.html — applyPaths, extractDest, extractDestPath, isExtractArchivePath</read_first>
  <action>В `applyPaths` (или сразу после маршрутизации в архив): если в payload **один** путь и `isExtractArchivePath(normalized)` истинно, выставить `extractDest =` родительский каталог этого пути (в JS: нормализовать строку, отрезать последний сегмент после `/` или `\`; для корня диска Windows не ломать). Обновить `extractDestPath.textContent`. Не перезаписывать, если пользователь уже вручную выбирал папку в этой сессии (опциональный флаг `extractDestUserChosen` — если проще, в первой версии всегда подставлять при новом open-files). Синхронизировать критичную логику с `ui-android/index.html`, если там тот же `applyPaths`.</action>
  <verify>Ручная проверка в dev: emit/open-files с одним `.zip` — поле назначения заполнено родителем.</verify>
  <acceptance_criteria>
    - При одном архиве в open-files extractDest заполняется dirname без ошибок для путей с пробелами
    - Дублирующие изменения в ui-android при наличии идентичной функции
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Проверка Windows argv и заметка в RESEARCH</name>
  <files>.planning/phases/04-associations-double-click/04-01-RESEARCH.md, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/lib.rs — ветка Windows/Linux Ready</read_first>
  <action>Если в коде есть ограничение «только существующие пути» — убедиться, что путь с архивом с диска проходит. При необходимости добавить в RESEARCH.md раздел «Проверено/Требует теста на Windows». Не менять логику без воспроизводимой ошибки.</action>
  <verify>Резюме в RESEARCH не противоречит коду.</verify>
  <acceptance_criteria>
    - В RESEARCH есть явная строка о поведении args() на Windows
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: README — ссылка на документацию ассоциаций</name>
  <files>README.md</files>
  <read_first>README.md — структура секций</read_first>
  <action>Добавить один абзац или строку в раздел про GUI/установку: «Ассоциации файлов и двойной щелчок: см. docs/FILE-ASSOCIATIONS.md».</action>
  <verify>Grep по README находит FILE-ASSOCIATIONS.</verify>
  <acceptance_criteria>
    - README содержит относительную ссылку на docs/FILE-ASSOCIATIONS.md
  </acceptance_criteria>
</task>

</tasks>

<verification_notes>
После выполнения: заполнить `04-01-SUMMARY.md` (шаблон GSD), обновить `.planning/STATE.md` — фаза 4 plan 01 complete.
Ручная проверка двойного щелчка — минимум 2 ОС из трёх (из success criteria ROADMAP).
</verification_notes>
