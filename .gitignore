# Dependencies & build
node_modules/
**/target/
dist/
out/
/fuzz/artifacts/
/fuzz/corpus/
/fuzz/Cargo.lock

# proptest (локальные сиды сбоя)
**/*.proptest-regressions

# OS / IDE
.DS_Store
__MACOSX/

# Локальный zip экспорта исходников (scripts/export-source-for-windows.sh)
OmegaZip-source-for-windows.zip
*.swp
.idea/
.vscode/

# Env & local
.env
.env.*

# YC prod secrets / state (local only)
infra/yc-static/terraform.tfvars
infra/yc-static/.yc-outputs.env
infra/yc-static/.terraform/
**/*sa-key*.json
.deploy.secrets.env
key_copy
key_copy.*
.tools/

# Local AI / editor tooling (not part of OmegaZip product)
.claude/

# Optional local benchmark corpus (CORPUS_EXTRA); do not commit
Архивы/

# Terraform state (secrets; keep local only)
infra/yc-static/terraform.tfstate
infra/yc-static/terraform.tfstate.backup
infra/yc-static/.yc-outputs.env

# Local artifacts / bulky outputs (not source of truth)
backups/
OmegaZip-for-review.tar.gz
archive.oz
test.oz
test.txt
test.extract.command
PROJECT_AUDIT_SUBMISSION.txt
PROJECT_DESCRIPTION_AUDIT.txt
