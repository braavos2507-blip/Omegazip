# Скопируйте в ~/.config/omegazip/auto_upgrade_folder_mb (одна строка, число мегабайт).
# При пресете auto: если сжимается папка и du >= N MB, используется --preset max.
# 0 — отключить автоповышение.
# Если файл отсутствует, в workflow по умолчанию 200.
200
